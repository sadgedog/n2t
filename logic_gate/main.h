#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <errno.h>

int nand(int a, int b);
int not(int in);
int and(int a, int b);
int or(int a, int b);
int xor(int a, int b);
int *not16(int in[16]);
